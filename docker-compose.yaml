version: "3.9"

services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
        - .:/app

    environment:
      NODE_ENV: development
    ports:
        - "5173:5173"
    command: npm run dev